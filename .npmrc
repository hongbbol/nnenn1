# @vercel/speed-insights가 여러 프레임워크용 optional peer(@sveltejs/kit 등)를 선언해
# 이 프로젝트의 vite와 npm strict peer 해석이 충돌한다. React 19 RC 조합과도 맞물리므로
# legacy-peer-deps로 설치한다. (Vercel 배포의 npm install도 이 설정을 따른다.)
legacy-peer-deps=true
